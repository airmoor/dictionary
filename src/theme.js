import { createMuiTheme } from '@material-ui/core/styles';
 const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#4062BB',
    },
    secondary: {
      main: '#ff7b86'
    },
    background: {
      paper: '#EBEBEB',
      default: '#59C3C3'
    }
  },  
});

export default theme;